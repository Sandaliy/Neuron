# apps/api

The Hono server that handles accounts and sync. Not scaffolded yet, it arrives with the first
endpoint.

Until then this directory holds nothing but this note, so that the workspace layout is visible from
the start.
