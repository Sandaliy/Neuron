# Design principles

Replaced by [design-system.md](design-system.md).

This file described the phase 5 visual layer: seven spacing steps, three radii, two durations, and a
token file with `--bg`, `--surface` and `--text` in it. Phase 5.5 replaced all of it with the approved
system, so every value here was wrong and a file of wrong values is worse than no file.

What used to be here, and where it is now:

| Was                        | Now                                                              |
| -------------------------- | ---------------------------------------------------------------- |
| The colour tokens          | design-system.md, Tokens. Two layers, and what each name means   |
| The type rules             | design-system.md, Typography and the type scale                  |
| The spacing and radii      | design-system.md, Scales                                         |
| The motion rules           | design-system.md, Motion rules                                   |
| The component list         | design-system.md, Component inventory, and `/dev/components`     |
| The accessibility promises | design-system.md, Accessibility. Measured, and printed by a test |
